# Generated by Cargo
# will have compiled files and executables
/target/
/gen/schemas

# Node sidecar binary and the assembled standalone server -- both are
# build-time outputs (see ../scripts/prepare-tauri-server.mjs and
# ../scripts/prepare-tauri-node-sidecar.mjs), never source, and the node
# binary alone is well over 100MB.
/binaries/
/resources/
