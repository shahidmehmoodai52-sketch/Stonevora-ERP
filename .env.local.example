NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Used for metadataBase, sitemap.xml and robots.txt once a production domain
# exists, and by the Capacitor mobile shell (capacitor.config.ts) to know which
# hosted URL to point the native app at.
NEXT_PUBLIC_SITE_URL=
